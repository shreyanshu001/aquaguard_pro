org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=false

# If Gradle can't find Java on this machine, you can set a project-local JDK path
# (uncomment and update the path to a valid JDK on your machine). Use double
# backslashes on Windows paths. Example:
# org.gradle.java.home=C:\\Program Files\\Java\\jdk-17.0.8

# Prefer setting a system-wide JAVA_HOME instead of committing a hard-coded
# path. See repository README or run the PowerShell snippet below to detect
# and set JAVA_HOME for the current machine.
